<h3><%= @bus.name %></h3>
<h4>Stops:</h4>

  <ul>
    <% @bus.bus_stops.each do |s| %>
      <li>
        <%= s.stop_name %> : <%= s.time %>
      </li>
    <% end %>
  </ul>

<p>
  <%= link_to 'Edit', edit_bus_path(@bus) %>
</p>

<p>
  <%= link_to 'Delete', bus_path, method: :delete  %>
</p>
